# 显式依赖(核心):省略号外的由 docling 等自动携带
fastapi
uvicorn
httpx
pydantic-settings
pydantic-ai-slim[openai]==1.107.5
sqlalchemy>=2.0
python-multipart

# 向量检索
qdrant-client>=1.12,<1.14
protobuf>=6.30.2,<7
grpcio-tools==1.76.0

# 解析(Docling 统一)
docling
pymupdf
onnxruntime

# 文档导出
python-docx
python-pptx
openpyxl

# 数值/检索
numpy

# 数据库
psycopg[binary]

# Graph query projection (PostgreSQL remains the audit source of truth)
neo4j>=6.0
